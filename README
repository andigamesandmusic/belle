1) Clone the repo:
git clone git://github.com/burnson/Belle.git

2) Change directory to Belle:
cd Belle

3) Initialize and update the submodules:
git submodule init
git submodule update

4) Try making all the examples:
Scripts/MakeAll
